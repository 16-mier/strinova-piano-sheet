# -*- coding: utf-8 -*-
"""配色与尺寸 —— 悬浮在游戏画面上，必须够醒目、又不能太挡视线。"""

from PyQt6.QtGui import QColor

# 面板背景（半透明深色）
BG = QColor(10, 12, 18, 168)
BG_EDGE = QColor(96, 106, 138, 205)

# 空闲格子
CELL = QColor(46, 52, 70, 200)
CELL_EDGE = QColor(98, 108, 140, 215)

# ★ 当前该打的键 = **深色**，不是亮黄 ★
#   用户：「我的意思是当前按的按键是**深色**的，并且**按下去的时候
#   要闪烁一下**」。
#
#   原来的做法是让它**一直亮黄**（`255,206,48`）—— 那是"进度条"的思路：
#   靠一块很抢眼的色块告诉你"现在打这个"。
#   可实战里它有两个毛病：
#     · 一直亮着反而让人**分不出"我按了没有"** —— 按下去它没变化，
#       因为你按不按它都是那个样子
#     · 黄色太抢眼，压住了"后面几个音"的预告
#
#   改成"深色打底 + 按下去闪一下"之后，反馈是**事件驱动**的：
#   平时是一块低调的深色（看得见"该这个"，但不刺眼），
#   手指落下的一瞬间它闪一下 —— 那一下就是"你按对了"的确认。
ACTIVE = QColor(96, 108, 74, 235)        # 深橄榄（跟黄色同色系，但压暗）
ACTIVE_EDGE = QColor(186, 202, 150)      # 亮一点的边，保证轮廓清楚
ACTIVE_TEXT = QColor(238, 244, 226)      # 深底上用浅字（原来是深字配亮黄底）

# ★ 浅底上用的深字 ★
#   用户报过：连按的当前格上，音名**看不见了**。
#   不是被谁盖住 —— 是上面那个 `ACTIVE_TEXT`（浅奶油 238,244,226）
#   压在 `UPCOMING[0]` 的淡黄 (255,238,158) 上，两个亮度几乎一样，
#   等于白字写白纸。
#   那个色本来就是给深橄榄底配的（看它自己的注释），
#   可**前面几个预告格和连按的当前格都是浅底**。
#   所以按底色亮度分流：浅底用这个，深底才用 `ACTIVE_TEXT`。
#   色相取深棕（跟 `×N` 徽章的字色同族），压在黄底上不刺眼。
DARK_TEXT = QColor(58, 46, 10)

# 「按下去闪一下」用的那一下高亮（`flash` 走的就是这个色）
PRESS = QColor(255, 214, 74)             # 按下去的瞬间：亮黄，一下就过去

# ★ 「你已经按了」的反馈色（`_paint_flash` 用，跟 `PRESS` 是**两回事**）★
#
#   用户实测后报：「闪烁的按键配色不对需要改，**感觉那个按下去的按键
#   跟最后一个一样**」。
#
#   拆开看就明白了 —— 屏幕上所有黄色系的东西本来都在说同一件事
#   （"谱面提示"）：预告格淡黄 (255,238,158)、圆圈亮黄 (255,214,74)、
#   序号 `1` 也是亮黄。而"**按下去闪一下**"当时也是 `PRESS` 那个亮黄，
#   于是它跟"最后一个预告格"几乎分不出来 ——
#   一个在说"你刚按的"，一个在说"接下来要按的"，却是同一个颜色。
#
#   所以给"你按了"单独一个色相。选青绿的理由：
#     · 黄色系在色轮的另一头，**放大缩小时都不会跟任何一种黄混**；
#     · 项目里以前这层本来就是青色的（`views.py` 的历史注释记着），
#       只是中间为了"统成同一个颜色"改掉了 —— 现在证明那个统一是错的；
#     · 深底 (`ACTIVE` 深橄榄) 和浅底的预告格上，青绿的对比度都够。
#
#   一句话的分工：**黄 = 谱面告诉你该弹什么，青 = 你弹了。**
HIT = QColor(70, 235, 190)

# 后续音符 —— 越远越淡
UPCOMING = [
    QColor(255, 238, 158, 215),
    QColor(222, 212, 152, 180),
    QColor(192, 188, 152, 150),
    QColor(164, 164, 152, 122),
    QColor(146, 150, 154, 100),
]

TEXT = QColor(234, 238, 248)
TEXT_DIM = QColor(154, 162, 182)

# 音区配色（时间轴上的方块按音高所在区取色，见 `timeline_edit.py`）
# 注：下落式 `FallView` 已经砍掉（`views.py`），这里不再有第二个用户。
ZONE_COLORS = [
    QColor(120, 200, 255),   # 中音区
    QColor(140, 235, 190),   # 中音高段
    QColor(255, 190, 120),   # 高音区
    QColor(240, 150, 220),   # 倍高音
]

RADIUS = 10
GAP = 6


def zone_of(row: int) -> int:
    """格子所在行 -> 音区序号（0 最下）。"""
    return max(0, min(3, row))
