# ---- Python ----
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# ---- 打包产物 ----
build/
dist/
# （`卡丘琴谱器.spec` **要**提交：它是手写的构建配置，不是自动生成的）

# ---- 运行时生成的 ----
config.json
config.json.bak*
_crash.log
*.err
*.out

# ---- 生成物 / 旧备份 ----
#   `core/note_cnn.*` 是 `tools/train_note_cnn.py` 训出来的模型，
#   属于已经整套删掉的"听音识谱"（见 DEVELOPMENT.md §16.38）——
#   主程序没有任何一行代码读它，所以别把二进制塞进仓库。
#   `DEVELOPMENT_v1_backup.md` 是手册改版前的备份，留着占地方。
core/note_cnn.npz
core/note_cnn.json
DEVELOPMENT_v1_backup.md

# ---- 开发期间的临时截图 / 日志 / 探针 ----
#   ★ 一律以下划线开头 ★
#     这个约定本来就在用（`_audit_code.md`、`_ring_01_approach.png`、
#     `_sweep_*.txt`……），只是原来没写进 .gitignore，于是
#     147 项里有一多半是这些东西。开源之前统一收掉。
_*
preview_*.png
shot_*.png
eval_drift.png

# ---- 音频样本 ----
#  ★ `assets/notes/` 那 16 个音源**要**进仓库 ★（2026-09 用户定的）
#    理由：打包版和源码版必须同一个音色，别人 clone 下来也该听到一样的。
#    它们是从卡拉彼丘 `.pck` 里提取出来的音频（见 DEVELOPMENT.md §7），
#    §16.56 一度判为"不公开分发"摘掉了，§16.61 用户看过风险之后决定放回来。
#  ★ 另外三套不进 ★
#    `notes_raw/`（QOA 原始块）、`notes_game/`（同一批采样的另一种导出）、
#    `notes_synth/`（合成音的中间产物）—— 程序一行都不读，
#    纯属开发期素材。真正在用的是 `notes/` 那一套。
assets/notes_raw/
assets/notes_game/
assets/notes_synth/
*.wav
!assets/notes/*.wav
# ★ 手机版那份音源是**复制**过来的，不进仓库 ★
#   `web/assets/notes/` 和 `assets/notes/` 是同一批 16 个 wav。
#   提交两份的话迟早会漂移（换了一个音色、只改了其中一边），
#   而且是 2.4 MB 的纯重复。
#   由 `mobile/build_apk.ps1` 在构建前拷过去；
#   想在浏览器里直接开 `web/` 的话，手动跑一次那个拷贝命令就行
#   （见 `web/README.md`）。
web/assets/notes/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
Thumbs.db
desktop.ini

# ---- 手机版（Capacitor → 安卓 APK）----
#  ★ 哪些进仓库 ★
#    `web/`（网页应用本体）、`mobile/package.json`、
#    `mobile/capacitor.config.json`、`mobile/build_apk.ps1` —— 都要提交，
#    它们是"源码"。
#  ★ 哪些不进 ★
#    `mobile/android/` 是 `cap add android` 生成的（几百个文件），
#    但 Capacitor 的惯例是**提交它** —— 因为改过的原生配置
#    （图标、权限、包名）就存在里面，不提交的话下次 `cap add` 会覆盖掉。
#    所以只忽略它下面的构建产物和自动生成的部分。
mobile/node_modules/
mobile/android/.gradle/
mobile/android/build/
mobile/android/app/build/
mobile/android/capacitor-cordova-android-plugins/
# `cap sync` 每次都会把 web/ 原样复制到这里，是**生成物**
mobile/android/app/src/main/assets/public/
mobile/android/app/src/main/assets/capacitor.config.json
mobile/android/local.properties
*.apk
*.aab
